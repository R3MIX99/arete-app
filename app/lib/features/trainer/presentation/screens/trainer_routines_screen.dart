import 'package:flutter/material.dart';

import '../../../../core/widgets/placeholder_screen.dart';

/// Creación y asignación de rutinas de entrenamiento.
class TrainerRoutinesScreen extends StatelessWidget {
  const TrainerRoutinesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderContent(
      title: 'Rutinas',
      subtitle: 'Aquí crearás y asignarás rutinas de entrenamiento.',
    );
  }
}
